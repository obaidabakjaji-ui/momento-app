<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Location lock (Phase 4): foreground-only reads during posting. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".HuddlexApp"
        android:label="Huddlex"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Huddlex"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Momento home screen widget (Phase 7) — mirrors the Flutter
             manifest's declaration; only the class package differs
             (.widget.MomentoWidgetReceiver). Exported so the launcher and
             the receiver's own self-armed expiry alarm can deliver
             APPWIDGET_UPDATE / ACTION_EXPIRY_TICK broadcasts. -->
        <receiver
            android:name=".widget.MomentoWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/momento_widget_info" />
        </receiver>
    </application>
</manifest>
