<?xml version="1.0" encoding="utf-8"?>
<!--
  Huddlex home-screen widget root layout.

  Three child surfaces, exactly one visible at a time depending on how many
  active posts the receiver decides to render:
    * widget_single_card — static (no animation) when count == 1
    * widget_flipper     — cross-fading ViewFlipper when count > 1
    * widget_empty       — friendly empty state when count == 0

  The static surface is the trick to keep iOS-style cross-fade on the
  multi-photo case without re-introducing the single-photo glitch (a
  ViewFlipper plays inAnimation / outAnimation every flipInterval even when
  the "next" child is itself, which read as a continuous fade-to-nothing).
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Multi-photo flipper. Cross-fade animations re-enabled now that
         single-photo case bypasses the flipper entirely. -->
    <ViewFlipper
        android:id="@+id/widget_flipper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:autoStart="true"
        android:flipInterval="3000"
        android:inAnimation="@android:anim/fade_in"
        android:outAnimation="@android:anim/fade_out"
        android:visibility="gone" />

    <!-- Single-photo static card. Structure mirrors widget_photo_card.xml
         so the visual is identical; only the IDs differ (Android requires
         unique IDs per layout). -->
    <FrameLayout
        android:id="@+id/widget_single_card"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_background"
        android:visibility="gone">

        <ImageView
            android:id="@+id/single_card_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:contentDescription="Huddle photo" />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="96dp"
            android:layout_gravity="bottom"
            android:background="@drawable/widget_bottom_scrim" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="8dp"
            android:paddingBottom="10dp">

            <TextView
                android:id="@+id/single_card_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="13sp"
                android:textStyle="bold"
                android:shadowColor="#80000000"
                android:shadowDx="0"
                android:shadowDy="1"
                android:shadowRadius="4"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/single_card_caption"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="#D0FFFFFF"
                android:textSize="11sp"
                android:maxLines="1"
                android:ellipsize="end"
                android:layout_marginTop="2dp"
                android:visibility="gone" />
        </LinearLayout>
    </FrameLayout>

    <LinearLayout
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/widget_background"
        android:gravity="center"
        android:orientation="vertical"
        android:visibility="visible">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Huddlex"
            android:textColor="#2D2337"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Your communities are waiting"
            android:textColor="#802D2337"
            android:textSize="12sp"
            android:layout_marginTop="4dp" />
    </LinearLayout>

</FrameLayout>
